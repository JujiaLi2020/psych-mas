# Examinee 332 evidence packet

This packet contains records extracted from the saved SQLite run with no
recomputation. The focal path is:

`rg_CUMP + rg_NT -> RT moderate`

`tt_EDI_SD + tt_GBT_SD -> TP strong`

`RT + TP -> Critical / Expedited`

`EDI_SD_NO / CO / TS` remains one `tt_EDI_SD` aggregation family. The raw
variant rows remain available in `b3_input.csv` for audit, but they should not
be counted as separate TP families in the tutorial narrative.
